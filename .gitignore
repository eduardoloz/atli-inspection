# Secrets — never commit
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Datasets, training runs, weights — regenerate from Roboflow + data/build_dataset.py
datasets/
runs/
*.pt
*.pth

# Conference paper (copyrighted — cite it, don't redistribute)
APET2025_T0388.pdf

# PI's paper folder (copyrighted PDFs + local data/results dumps)
ATLI/
*.zip

# Hard-negative review image dumps (regenerate via analysis/hard_negatives/)
analysis/hard_negatives/review/
analysis/hard_negatives/review_annotated/

# Bulky regenerable vetting/comparison image montages (findings kept in the .md docs + .json)
results/classvet_review/review/
results/classvet_review/pairs/
results/box_compare/

# Unrelated side project (relocate out of this repo when convenient)
amazon-order-tracker/

# Personal communications — never commit
results/email_*.md
results/update_*.md

# Local tooling / OS cruft
.claude/
.DS_Store

# private drafts / redacted content — never commit
results/email_to_PI*.md
results/LOCAL_*
results/data_outreach*.md
*.pdf

# Dedupe backups + regenerable duplicate-review contact sheets (plan/findings
# committed in rebalance/eduardo_dedupe_plan.json + results/eduardo_dedup_audit.md;
# pixels frozen in Roboflow version v5)
rebalance/backups/
results/eduardo_dupes_review/
